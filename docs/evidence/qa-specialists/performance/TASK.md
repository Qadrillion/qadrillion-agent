Use .cursor/skills/qa/SKILL.md as the QA entry point and only relevant instructions. Work solely in this disposable workspace and its supplied local target. Characterize the local parcel /work operation against tools/specialists/CONTRACT.md using the provided bounded helper tools/specialists/measure.py. Before measurement set 50 measured requests, 1 worker, 3 warmups, timeout1s, duration10s, max3errors, p95<=40ms and error_rate=0. Author a repeatable invocation/check wrapper, run it and analyze samples, thresholds, stops, workload and limits. No production-capacity claim, other endpoints, broad load, or fixture modifications. Baseline will be supplied separately; do not fabricate it. Target identity is target.json; verify it live before mutation. Task authorization includes routine test authoring, local execution, dependencies and owned fixture cleanup; no external publication. Do not change instruction/config files or target identity. Keep output in artifacts/ and tests in tests/. Record command, exit/counts, environment, cleanup, unexecuted scope and a concise result with artifact paths. Leave tests ready to rerun unchanged against another identity file. Do not stop at a plan.
